name: move-v1-tag

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  move-v1-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: |
          git tag -f v1
          git push -f origin v1
